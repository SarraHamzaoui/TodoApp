name: CI - Tests, Coverage, Security

on:
  push:
    branches: [ main ]
  pull_request: # Déclencheur normal pour les tests
    branches: [ main ]
  workflow_dispatch:

jobs:

  build-and-test:
    runs-on: ubuntu-latest

    # ✅ Le bloc permissions est CORRECTEMENT placé ici
    permissions:
      contents: read
      actions: write
      security-events: write

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: tododb
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=5s
          --health-timeout=2s
          --health-retries=20

    steps:
      - name: ⚙️ Checkout repository
        uses: actions/checkout@v4

      - name: 🛠️ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: ⏳ Wait for MySQL to be ready
        run: |
          for i in {1..30}; do
            mysql -h 127.0.0.1 -u root -proot -e "SELECT 1" && break || sleep 2
          done

      - name: 🏗️ Run Maven Build & Tests
        run: mvn clean install -B
        working-directory: TodoBackend/TodoApp

      - name: 📊 Generate Surefire HTML report
        run: mvn surefire-report:report
        working-directory: TodoBackend/TodoApp

      - name: 📝 Upload test reports
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: |
            TodoBackend/TodoApp/target/site/surefire-report.html
            TodoBackend/TodoApp/target/surefire-reports/**

      - name: 📝 Upload JaCoCo Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: TodoBackend/TodoApp/target/site/jacoco/

  security-scan:
    runs-on: ubuntu-latest
    needs: build-and-test

    # ✅ CRITIQUE : Le job a besoin de "write" pour uploader le rapport SARIF
    permissions:
      contents: read
      actions: write
      security-events: write

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: tododb
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=5s
          --health-timeout=2s
          --health-retries=20

    steps:
      - name: ⚙️ Checkout repository
        uses: actions/checkout@v4

      - name: 🛠️ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: 🚀 Start Spring Boot (background) via JAR
        working-directory: TodoBackend/TodoApp
        run: |
          # Trouver le nom du JAR compilé
          JAR_FILE=$(ls target/*.jar | head -n 1)
          
          if [ -z "$JAR_FILE" ]; then
            echo "Error: JAR file not found in target/. Aborting scan."
            exit 1
          fi

          echo "Starting application from $JAR_FILE on port 8089..."
          
          # Démarrer le JAR en forçant la configuration DB et le port
          nohup java -jar $JAR_FILE \
            --server.port=8089 \
            --spring.datasource.url="jdbc:mysql://localhost:3306/tododb" \
            --spring.datasource.username=root \
            --spring.datasource.password=root > springboot.log 2>&1 &
          
          echo "Waiting 45 seconds for server startup..."
          sleep 45 
          
          echo "--- Spring Boot Logs ---"
          tail -n 30 springboot.log
          echo "------------------------"

      # 🔍 NOUVELLE VÉRIFICATION DE SANTÉ PLUS ROBUSTE
      - name: ⏳ Wait for App to be Responsive
        run: |
          echo "Waiting for http://localhost:8089/auth/login to be responsive..."
          for i in {1..10}; do
            if curl -s -o /dev/null -w "%{http_code}" http://localhost:8089/auth/login | grep -q '40'; then
              echo "App responded. Starting in 3s..."
              sleep 3
              break
            fi
            echo "Still waiting for port 8089..."
            sleep 5
          done

      # 2️⃣ Créer l'utilisateur (on utilise les mêmes infos que pour la connexion)
      - name: 👤 Register Scan User
        run: |
          curl -s -X POST http://localhost:8089/auth/register \
            -H "Content-Type: application/json" \
            -d '{"email":"scanuser@zap.com","password":"zap-password"}' || true

      # 3️⃣ Récupérer le JWT token (avec les identifiants synchronisés)
      - name: 🔑 Get JWT token
        id: get-token
        run: |
          RESPONSE=$(curl -s -X POST http://localhost:8089/auth/login \
            -H "Content-Type: application/json" \
            -d '{"email":"scanuser@zap.com","password":"zap-password"}')
          
          TOKEN=$(echo "$RESPONSE" | jq -r '.token')
          
          if [ -z "$TOKEN" ] || [ "$TOKEN" = "null" ]; then
            echo "ERROR: Failed to get JWT token. Response from server:"
            echo "$RESPONSE"
            exit 1
          fi
          echo "TOKEN=$TOKEN" >> $GITHUB_ENV

      # 4️⃣ Exécuter ZAP (SARIF + HTML)
      - name: 🛡️ Run OWASP ZAP Baseline Scan (Authenticated, Generate SARIF + HTML)
        uses: zaproxy/action-baseline@v0.7.0
        with:
          target: 'http://localhost:8089'
          docker_name: 'ghcr.io/zaproxy/zaproxy:stable'
          # Le rapport SARIF est généré par défaut.
          # Le rapport HTML est forcé à être écrit dans le répertoire courant (./zap-report.html)
          cmd_options: >-
            -a --autooff 
            -z "-config replacer.full_list_of_rules.rule_0.description=AuthHeader -config replacer.full_list_of_rules.rule_0.enabled=true -config replacer.full_list_of_rules.rule_0.matchtype=REQ_HEADER -config replacer.full_list_of_rules.rule_0.matchregex=false -config replacer.full_list_of_rules.rule_0.replacement=Authorization: Bearer ${{ env.TOKEN }} -config replacer.full_list_of_rules.rule_0.urlregex=.* -config replacer.full_list_of_rules.rule_0.initiators=2,4,6,8,11,13,14,15,16,17"
            -r zap-report.html # Génère le rapport HTML à la racine de l'espace de travail

      # 5️⃣ Uploader le rapport SARIF à GitHub Code Scanning (Méthode officielle)
      - name: ⬆️ Upload SARIF report to GitHub
        if: github.event_name == 'push' || github.repository == github.event.pull_request.base.repo.full_name
        uses: github/codeql-action/upload-sarif@v3
        continue-on-error: true # Maintient la CI verte même si l'erreur de permission survient sur un fork.
        with:
          sarif_file: 'zap-sarif-report.sarif'
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 📝 Upload ZAP HTML Report (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: zap-security-report
          path: ./zap-report.html # Le chemin est maintenant garanti correct